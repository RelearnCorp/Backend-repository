meta {
  name: Classes Enroll
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/classes/enroll
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  "class_code": "replace-with-class-code"
}
